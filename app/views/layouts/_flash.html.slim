- flash.each do |name, msg|
  div class="alert alert-#{name.to_s == 'notice' ? 'success' : 'danger'}"
    button.close[type='button' data-dismiss='alert' aria-hidden='true'] ×
    = content_tag :div, (msg.is_a?(Array) ? msg.join('<br>') : msg), id: "flash_#{name}"